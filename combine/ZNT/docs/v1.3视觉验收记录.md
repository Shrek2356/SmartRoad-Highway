# v1.3 视觉与回归记录

日期：2026-09-07。范围：PC 管理后台与 Windows 桌面外壳。未更改手机端／大屏，不改变推理策略；本次不运行 GPU 模型或云端检测。

## 自动检查

- 前端：17 项测试通过，包括原有权限、导航、连接、展示素材、图片框几何测试及 6 项新增主题回归。
- 后端与桌面：145 项测试通过，1 项既有 warning，22.77 秒。
- 核心色板：明暗主题的正文、辅助文字、链接、风险文本与底色对比度至少 4.5:1；主按钮与危险按钮前景另行检查。该结果是色板级检查，不代表完整页面可访问性认证。
- 前端构建成功，保留既有的较大代码分块警告；本次未为压缩体积拆改功能。
- 对照 v1.2 交付包，`pc-admin/public` 的 55 个文件均在，哈希全部一致。页面结果汇总显示 8 个预置案例。

## 人工视觉检查

在本地独立测试页面检查登录、工作台明暗切换、深色实时检测、结果汇总、复盘图表、模型配置与使用指南。确认卡片、表格、图表文字、选中模式、状态标签、滑杆与侧栏可读。720 像素高窗口侧栏能看到全部菜单。

测试仅访问已有案例和页面，未修改业务阈值、模型路径、API 配置或检测结果。测试页面使用单独的本地端口；未停止用户原先打开的 v1.2 程序。

## 正式交付包校验

以下为 2026-09-07 正式包的验收摘要，便于从 GitHub 查看；原本地交付目录中的 `v1.3交付验收.md` 保留完整记录。

| 项目 | 已记录结果 |
|------|------------|
| 正式文件名 | `SiteSafe-Sentinel_Desktop_v1.3.0_visual_final.zip` |
| 大小 | 236,095,515 字节，225.16 MiB |
| SHA-256 | `1279FCB257015054786D0DFC84AFE20C7984BB3AFBCB6DB701065078768067A5` |
| 独立解压检查 | 5,524 项清单文件，0 项缺失或哈希不一致 |
| EXE 元数据 | 文件版本、产品版本均为 1.3.0 |
| 随包环境 | Python 3.12.10；FastAPI 0.141.1；Demo／RAG 所需依赖导入通过 |

这描述项目方提供的运行包，不是 GitHub 自动生成的源码 ZIP，也不表示已上传 GitHub Releases。新增说明未改写该压缩包，其哈希仍对应上述原始交付文件。

## 验证边界

当次验收时旧版 EXE 仍占用全局单实例，因此未强行关闭用户窗口，**新 EXE 没有完成独立窗口启动验收**。打开新版前应先正常退出旧版。这里是当次记录，不是对读者当前电脑进程状态的判断。

前端页面已进行视觉检查，但不等于在新 EXE 中人工复测了全部功能。本轮没有运行 GPU／云端模型，没有新的召回率、误报率或推理耗时结果。

GitHub 文档同步不包括本机临时运行日志、会话、私有模型配置或大模型权重；授权条款不变。后续如修改运行包内容，须重新计算校验值并建立新的验收记录。
