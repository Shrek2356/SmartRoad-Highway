# v1.2 产品体验与链路验收

日期：2026-09-07。

## 自动化回归

- Python：145 项通过，1 条依赖警告，25.72 秒；覆盖原有业务链路、桌面网关 HTTP/WebSocket 联调，以及新增启动错误说明和固定支持目录校验。
- 前端：11 项通过。新增验证全部 13 个操作页面的指南覆盖、角色权限一致性、快捷入口有效性、搜索、状态语义、诊断导出脱敏、检测意图保留和地址校验。
- Vite 生产构建成功。保留大依赖的分块体积提示，不以隐藏构建警告代替性能优化。

## 界面检查

通过真实本地服务，检查了登录、首次欢迎、跳过引导、功能搜索、从搜索进入知识库页签、本页说明、连接诊断、快速开始清单及恢复预置素材显示。

明亮与暗色界面均检查过。结果汇总中八个原有关键案例可见，并与测试窗口中的三条既有演示记录明确区分。本轮没有重新生成这些检测案例。

检查期间，测试窗口按预先设定的 20 分钟计时正常退出；此后的浏览器临时页面如显示连接断开是测试服务已关闭，并非模型故障。

## 验收边界

测试没有调用新的云端推理，没有启动 GPU 模型，没有重新计算检测率或耗时。回归和界面检查不能替代不同电脑上的完整部署测试、长期多摄像头负载测试或商业安全认证。

操作入口和说明已覆盖各业务模块；并不宣称每种硬件、流协议或异常输入都经过人工遍历。持续改进方向见产品使用指南。
