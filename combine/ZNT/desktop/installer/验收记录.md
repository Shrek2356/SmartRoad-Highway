# v1.4.1 Windows 安装版验收

日期：2026-09-07。本次只增加安装包装，不改模型推理、Agent 策略、前端效果或既有案例。

## 最终交付

- 文件：`SiteSafe-Sentinel_Setup_v1.4.1_x64.exe`
- 大小：465,235,595 字节，约 443.68 MiB。
- SHA-256：`088ED49B129BA1C83B671392E97B4A4D6D45A0703C23BEE4600C623DB43A35D3`
- 应用本体：v1.4.1；输入为已验证的 `SiteSafe-Sentinel_Desktop_v1.4.1_deployment_final.zip`。
- 注册类型：当前 Windows 用户，包含“已安装的应用”及卸载入口；不是联网账号注册/授权激活。

## 已完成验证

8 项打包逻辑单元测试和 19 项安装集成检查全部通过。

| 范围 | 结果 |
|---|---|
| 载荷边界 | 拒绝越界路径、重复条目、清单外文件、哈希损坏和私有运行数据 |
| 既有目录保护 | 拒绝覆盖其他资料目录，原有测试资料不变 |
| 运行互斥 | 持有桌面应用互斥标记时，安装器退出，不强制结束软件 |
| 新位置安装 | 含中文与空格的独立目录安装成功，不要求旧盘符或旧 Python 路径 |
| 软件登记 | 当前用户卸载注册位置正确，开始菜单四个入口创建，主快捷方式目标及工作目录正确 |
| 素材完整性 | 安装后的 5,544 个清单文件逐项哈希一致，含原有八张案例及相关前端素材 |
| 独立窗口 | Demo 档启动成功；5173 前端、8800 业务后台、8810 检测桥健康响应正常，登录页面可访问 |
| 关闭 | 定时窗口测试正常结束，5173/8800/8810/8080 没有残留监听 |
| 重装 | 同版本重装成功；修改后的模型初始配置、桌面配置、业务资料保持不变 |
| 卸载 | 程序、开始菜单入口和产品卸载登记移除；配置、业务库、测试规范、模型/环境目录及重装标记保留 |

本机已有 WebView2，安装器正确跳过共享运行环境安装。包内微软离线安装文件已验证为有效 Microsoft Corporation 签名，大小 258,614,480 字节。其余约 206.6 MB 为安装器与压缩后的应用、Python、源码及案例素材；包中没有模型权重。

测试使用独立目录及本次临时创建的开始菜单入口，没有改变原便携软件、既有桌面快捷方式或模型。临时安装已卸载，测试资料保留在测试区，不放进交付包。未调用云端 API 或运行大模型。

## 尚未证明的范围

- 没有在第二台物理电脑执行；不同驱动、权限、组织策略和系统环境仍需验收。
- 未移除本机 WebView2 来测试缺失分支；离线补齐逻辑已接入，但缺失环境实装需在干净 Windows 上验证。
- 实际测试的是同版本重装，不代表未来任意跨版本升级/降级均已验证。
- 桌面快捷方式为安装器可选项，本次自动测试未勾选，避免改变用户桌面；开始菜单快捷方式已实际验证。
- 本项目 EXE 和最终安装器均未代码签名；不能承诺没有 Windows 信誉提醒。微软 WebView2 原安装文件的签名不等于本项目安装器已签名。

完整模型部署仍以软件内的模型部署引导为准，模型效果沿用此前测试，不把安装验收当作新检测实验。
