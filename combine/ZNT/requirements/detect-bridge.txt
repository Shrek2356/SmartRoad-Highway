# 检测桥接最小依赖（模式 1 Demo + 规范知识库可用）
# 上限用于避免全新设备直接安装尚未验证的主版本。
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
python-multipart>=0.0.9,<1
python-dotenv>=1.0,<2
httpx>=0.27,<2
psutil>=5.9,<8
PyYAML>=6.0,<7
pydantic>=2.6,<3
Pillow>=10.0,<13
numpy>=1.24,<3

# RAG 规范导入、索引与检索。检测桥启动时会建立 TF-IDF 索引，
# 因此 scikit-learn 不是可选依赖；PDF/DOCX 是前端规范导入支持项。
scikit-learn>=1.3,<2
pypdf>=4.0,<7
python-docx>=1.1,<2
