# 比赛提交边界

- 最终检测主链以 MLLM 为开放风险识别核心，不包含早期废弃实验框架。
- 检测模块统一命名为 `Site_Safety_OpenRisk`。
- 不提交逐轮推理原文、调参过程、历史任务目录、服务日志、数据库和缓存。
- 保留八张关键案例、前端展示图、Mask、最终报告数据和必要 Agent 示例。
- 不包含 Qwen、YOLO、SAM3、CLIP 权重及第三方模型仓库。
- 包含仅供 Demo/业务后台/检测桥/RAG 使用的便携 Python 与 Node 运行时；不会在首次启动联网安装。
- 演示数据在前端明确标注；真实接口数据与演示素材不会混写。
